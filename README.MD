Hye is a web server for computer vision

create `uploads` folder and install requirements before running server

you need to send a `post` request with `image` and `hars` to / 

### hars implemented:
- [x] face
- [x] eyes